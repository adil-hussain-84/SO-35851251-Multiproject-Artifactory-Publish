apply plugin: 'java'

publishing {
    publications {
        SomePublication(MavenPublication) {
            groupId 'com.test'
            artifactId 'foo'
            version '1.0.0'
            from components.java
        }
    }
}