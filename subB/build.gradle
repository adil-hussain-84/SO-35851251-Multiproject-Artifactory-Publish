apply plugin: 'java'

publishing {
    publications {
        someOtherName(MavenPublication) {
            groupId 'com.test'
            artifactId 'bar'
            version '1.0.0'
            from components.java
        }
    }
}